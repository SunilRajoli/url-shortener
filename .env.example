# PostgreSQL connection (either DATABASE_URL or individual parts)
DATABASE_URL=postgres://user:password@localhost:5432/urlshortener

# Where to serve short urls from (used to construct short_url in responses)
BASE_URL=http://localhost:4000

# Length of generated short code
CODE_LENGTH=7

# Server port
PORT=4000
